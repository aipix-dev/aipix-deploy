# Payment webhook credentials
BILLING_PAYMENT_GETAWAY_LOGIN=admin
BILLING_PAYMENT_GETAWAY_PASSWORD=admin

# Portal url, need to setup with / at the end
PORTAL_URL=http://portal-backend:80/

# Database root credentials for first time DB creation
DB_ROOT_USERNAME=root
DB_ROOT_PASSWORD=mysql

# Database connections parameters
DB_HOST=mysql-server
DB_PORT=3306

