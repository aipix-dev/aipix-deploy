---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  labels:
    app.kubernetes.io/instance: traefik-traefik-v2
    app.kubernetes.io/name: traefik
  namespace: vsaas-vms
  name: embed-player
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host (`vms.example.com`) && PathPrefix(`/player/embed`)
      middlewares:
      - name: strip-prefix-embed-player
      services:
        - kind: Service
          name: embed-player
          port: 80